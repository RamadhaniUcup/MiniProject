export type MotifInfo = {
  classKey: string;
  title: string;
  origin: string;
  description: string;
  philosophy: string;
  image: string;
};

export type ScanResult = {
  classKey: string;
  title: string;
  origin: string;
  philosophy: string;
  confidence: number;
};
